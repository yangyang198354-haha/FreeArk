# 开发环境环境变量配置

# Django 安全密钥（必须在生产环境中修改为强随机字符串）
SECRET_KEY=4s)5+tp@t4uf%8h%bry%z!cbd*e-4wsfa&378-clgp*x7n&+_=

# 调试模式（开发环境设为 True 便于调试）
DEBUG=True

# 允许的主机
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# 数据库配置 - 使用本地开发数据库
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3

# CORS 配置 - 允许前端开发服务器
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost,http://127.0.0.1:8080,http://127.0.0.1

# HTTPS 配置（开发环境禁用）
SECURE_SSL_REDIRECT=False

# 日志级别
DJANGO_LOG_LEVEL=INFO
APP_LOG_LEVEL=INFO

# 工作进程数（可选，默认由 gunicorn_config.py 控制）
# WORKERS=4